To run this project, extract the zip, open the folder in terminal, run npm install, and then run npm run dev.
```

Open [http://localhost:3000](http://localhost:3000)utm_source=create-next-app&utm_campaign=create-next-app-readme) from the creators of Next.js.


